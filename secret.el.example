;;; .secret.el -*- lexical-binding: t; -*-

;; (setq my-keyboard-variant "colemak")                 ; set this here to avoid having to change the main configuration file


;; (setq set-initial-size nil)                          ; don't set the intial size of the frame

;; Merriam-Webster API key
(defconst my-mw-api-key "API-KEY-HERE")
